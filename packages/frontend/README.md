# Frontend

React + TypeScript + Vite frontend for Uniblox Store.

## Setup

```bash
pnpm install
pnpm dev:frontend
```

Runs on `http://localhost:5173` and proxies `/api` requests to the backend at `http://localhost:3001`.
